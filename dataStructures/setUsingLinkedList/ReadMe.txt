This is a continuation of the first data structures course assignment, this time backed by a linked list data structure using Java generics that is capable of ascending and descending sorting.

Main.java is the program itself, allowing users to create sets and perform several operations upon them.